
function randomNumber(){
    let nombre = [];
    for (i = 0 ; i < 13 ; i++){
        document.getElementById("code").appendChild.innerHTML = '<div class="c"></div>'
        nombre.push(Math.floor(Math.random() * 10))
        // document.getElementsByClassName("c").style.backgroundColor = "black";
        // document.getElementsByClassName("c").style.width += nombre[i] + "px";
    }
}

